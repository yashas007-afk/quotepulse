# Use an Nginx image to serve the static HTML
FROM nginx:alpine

# Copy custom HTML to the Nginx default directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 (Nginx default)
EXPOSE 80
